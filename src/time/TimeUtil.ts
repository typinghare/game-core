/**
 * Encompasses a bunch of utility functions related to time.
 */
export class TimeUtil {
    /**
     * Returns current timestamp.
     */
    static now(): number {
        return Date.now()
    }
}